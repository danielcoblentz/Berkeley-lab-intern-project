# Core dependencies (use a compatible Python 3.10+ runtime)
numpy>=1.24
pandas>=1.5
scikit-learn>=1.2
torch>=2.0
transformers>=4.35.2
datasets>=2.15.0
pytorch-tabnet>=0.3.0
tqdm>=4.65
umap-learn>=0.5.4
pytest>=7.0

# Optional / advanced (GPU or perf-sensitive):
# flash-attn must be installed with matching CUDA/toolkit; see requirements/README.md
# umap-learn often benefits from "pynndescent" backend
pynndescent>=0.6
